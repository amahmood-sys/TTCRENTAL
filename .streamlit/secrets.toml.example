# Copy this file to .streamlit/secrets.toml (which is gitignored) and set your
# own credentials, OR set the same [auth] section in the Streamlit Cloud
# "Secrets" panel. If no secrets are configured, the app falls back to the
# built-in defaults (username "aayan").

[auth]
username = "aayan"
password = "123456"
